{
	"name": "wrangler",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy index.js",
		"dev": "wrangler dev --port $PORT --show-interactive-dev-session=false index.js",
		"test": "vitest"
	},
	"dependencies": {
		"itty-router": "^2.6.1"
	},
	"devDependencies": {
		"wrangler": "^3.0.0"
	}
}
